// This tells the build tool (PostCSS) to run Tailwind's CSS processor.
// It's boilerplate that Tailwind's own setup docs ask for -- you shouldn't
// need to touch this file.
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
